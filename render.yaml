services:
  - type: web
    name: ga-domain-intel
    runtime: python
    buildCommand: echo "No build needed"
    startCommand: python server.py
    plan: free
    envVars:
      - key: RENDER
        value: "true"
